info:
  name: Check Session
  type: http
  seq: 4

http:
  method: GET
  url: https://api.onepeloton.com/auth/session
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
